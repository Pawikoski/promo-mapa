{
  "name": "olx-map",
  "version": "1.0.0",
  "main": "content.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=dist/injected.js --format=iife --target=es2020 --loader:.css=text",
    "watch": "esbuild src/index.js --bundle --outfile=dist/injected.js --format=iife --target=es2020 --loader:.css=text --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.27.3"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "leaflet.markercluster": "^1.5.3"
  }
}
